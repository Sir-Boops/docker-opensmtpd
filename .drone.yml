pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/opensmtpd
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
